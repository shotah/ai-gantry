#!/usr/bin/env bash
# Linux/macOS stub — Windows uses remote-native.ps1 via make.
# For now, prefer the PowerShell script from a Windows workstation, or run
# the same steps by hand (see deploy/README.md).
set -euo pipefail
echo "remote-native.sh: use scripts/remote-native.ps1 on Windows, or follow deploy/README.md" >&2
exit 1
