# SAM vision/OCR lab — intentionally empty.
# gemma3:12b (Ollama) returns 400 if the request includes ANY tool schemas.
# Keep MEMORY_ENABLED=false and CRON_ENABLED=false in .env for the same reason.
# Full Tim stack archived as mcp.full.toml.
