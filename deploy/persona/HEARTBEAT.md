# HEARTBEAT.md

> Copy to `HEARTBEAT.md`.

# Keep empty (comments only) to skip periodic heartbeat work.
# Uncomment / add lines when you want proactive checks, e.g.:
# - Morning: glance at calendar
# - If a recurring brief is due, draft it (don't nag)
#
# Scheduled pushes can also use built-in cron tools (cron_schedule) when
# CRON_ENABLED=true — see docs/cron.md.
