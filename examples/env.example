# Required — OpenAI-compatible endpoint (Gemini, xAI, Ollama, …)
LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
LLM_API_KEY=
LLM_MODEL=gemini-3.5-flash
# LLM_MAX_TOKENS=4096             # completion cap (0 = provider default)
# LLM_REASONING_EFFORT=none       # Ollama/Qwen3.5: omit=thinking on; none=off (streamed as expandable italics)
# Tip: LOCAL_AGENT compose maps GEMINI_API_KEY → LLM_API_KEY for you.
# See examples/docker/.env.example

# Channel: telegram (default), discord, slack, or stdio (local REPL)
CHANNEL=telegram

# Required when CHANNEL=telegram — allowlist only, no pairing
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=
# TELEGRAM_ERROR_REPORTING=off  # off|error|warn — tee slog into the SAM chat

# Required when CHANNEL=discord — see docs/discord.md
# DISCORD_BOT_TOKEN=
# DISCORD_ALLOWED_USERS=

# Required when CHANNEL=slack — see docs/slack.md
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...
# SLACK_ALLOWED_USERS=

# Optional
# PERSONA_DIR=/persona
# DATA_DIR=/data
# MCP_MANIFEST=/etc/gantry/mcp.toml
# HISTORY_MAX_MESSAGES=200
# HISTORY_MAX_TOKENS=128000
# TOOL_RESULT_MAX_CHARS=6000
# TOOL_MAX_ITERATIONS=10
# SELF_NOTES_ENABLED=true
# TOOL_SCHEMA_MAX_TOKENS=0
# MEMORY_ENABLED=true
# MEMORY_BACKEND=builtin
# MEMORY_CONSOLIDATE_MINUTES=30
# CRON_ENABLED=true
# CRON_TZ=America/Los_Angeles
# CRON_MAX_JOBS=50
# CRON_TICK_SECONDS=15
# WATCH_ENABLED=true
# WATCH_MAX=50
# FEEDS_USER_AGENT=feeds-mcp/0.1 (you@example.com)
# X_BEARER_TOKEN=
# GOOGLE_MAPS_API_KEY=
# STREAM_REPLIES=true       # edit-in-place streaming (default)
# SHOW_THINKING=true        # CoT italics → expandable (default; needs STREAM_REPLIES)
# TOOL_TRACE=compact        # full|compact|off (needs STREAM_REPLIES)
# LOG_LEVEL=info
